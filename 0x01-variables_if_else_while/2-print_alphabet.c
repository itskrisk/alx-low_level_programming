#include <stdio.h>
#include <stdlib.h>

/**
 * main - return alphasbets kris
 *
 * Return: Always 0 (success)
 */
int main(void)
{
	int ch = 'n';

	while (ch <= 'z')
	{
		putchar(ch);
		ch++;
	}
	putchar('\n');
	return (0);
}
